using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class robinsKutCode : MonoBehaviour
{
    private int distance;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //distance =  Mathf.abs(floor(reticle xpos / reticle hspeed) - floor(target center / reticle hspeed));


    }
}
